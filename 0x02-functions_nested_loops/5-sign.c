#include "main.h"
/**
*print_sign - signs are printed based on a condition.
* @n: arguement has succeeded
*Return: 0 ,1 ,-1
*/

int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	} else if (n == 0)
}
		_putchar(48);
		return (0);
	} else if (n < 0)
	{
		_putchar('-');
	}
		return (-1);
}
