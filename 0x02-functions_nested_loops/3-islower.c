#include "main.h"
/**
*_islower - check if char is lowercase
*@c: character
*Return: returns 0 or 1 based on is it is uppercase or lowercase.
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
	return (0);
}

